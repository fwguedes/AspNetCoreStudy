@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        Cuisine : @Model.Cuisine
    </div>
    <div>
        <td><a asp-action="Detail" asp-route-id="@Model.Id">Details</a></td>
        <td><a asp-action="Edit" asp-route-id="@Model.Id">Edit</a></td>
    </div>
</section>